{
  "pluginAlias": "Omnik",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "ip": {
        "title": "Omnik-inverter IP address",
        "type": "string",
        "required": true,
        "default": ""
      },
      "pollInterval": {
        "title": "Poll interval (in minutes)",
        "type": "number",
        "required": true,
        "default": 5,
        "minimum": 1,
        "maximum": 15
      }
    }
  }
}